# Virtual host comentado temporalmente - usar DocumentRoot por defecto de XAMPP
# Para usar este virtual host, accede a: http://jobbrige-backend.local/api/login
# O cambia la URL en frontend/src/config/api.js a: http://jobbrige-backend.local/api
# Virtual host comentado temporalmente - usar DocumentRoot por defecto de XAMPP
# Para usar este virtual host, accede a: http://jobbrige-backend.local/api/login
# O cambia la URL en frontend/src/config/api.js a: http://jobbrige-backend.local/api
#<VirtualHost *:80>
#    ServerName jobbrige-backend.local
#    ServerAlias localhost
#    DocumentRoot "D:/xampp/htdocs/jobbrige/backend/public"
#    
#    <Directory "D:/xampp/htdocs/jobbrige/backend/public">
#        AllowOverride All
#        Require all granted
#        
#        Header always set Access-Control-Allow-Origin "http://localhost:3000"
#        Header always set Access-Control-Allow-Methods "GET, POST, PUT, PATCH, DELETE, OPTIONS"
#        Header always set Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With, Accept, Origin, X-CSRF-TOKEN"
#        Header always set Access-Control-Allow-Credentials "true"
#        Header always set Access-Control-Max-Age "86400"
#        
#        RewriteEngine On
#        RewriteCond %{REQUEST_METHOD} OPTIONS
#        RewriteRule ^(.*)$ $1 [R=200,L]
#    </Directory>
#    
#    RewriteEngine On
#    RewriteCond %{REQUEST_FILENAME} !-f
#    RewriteCond %{REQUEST_FILENAME} !-d
#    RewriteRule ^(.*)$ index.php [QSA,L]
    
#    ErrorLog "logs/jobbrige-error.log"
#    CustomLog "logs/jobbrige-access.log" common
#</VirtualHost> 